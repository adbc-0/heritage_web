/**
 * @description Sets minimum width of node when calculating layout.
 * Later node width is modified by separator depending on content length.
 * */
export const NODE_WIDTH = 80;
/** @description Sets height of node when calculating layout */
export const NODE_HEIGHT = 80;
/** @description Allows for easy calculation for drawing the lines */
export const VERTICAL_SPACE_BETWEEN_NODES = 70;

/** @description The smaller the factor the more important is the size of element when calculating spacing */
export const SPACING_ELEMENT_SIZE_FACTOR = 13;
export const SPACING_BASE_SIZE = 0.4;

export const NODE_BASE_WIDTH = 25;
export const NODE_SIZE_FACTOR = 10;
